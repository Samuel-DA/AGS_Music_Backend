services:
  - type: web
    name: agsstack-music
    runtime: node
    buildCommand: pip install yt-dlp && npm install
    startCommand: node server.js
    envVars:
      - key: YOUTUBE_API_KEY
        sync: false